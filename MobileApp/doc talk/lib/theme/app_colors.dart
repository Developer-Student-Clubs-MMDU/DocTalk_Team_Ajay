import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors for Light Theme
  static const Color primaryBlue = Color(0xFF4A90E2); // Soft Blue
  static const Color primaryTeal = Color(0xFF00897B); // Teal

  // Secondary Colors for Light Theme
  static const Color lightGray = Color(0xFFF5F5F5);  // Light Gray
  static const Color white = Color(0xFFFFFFFF);      // White

  // Accent Colors for Light Theme
  static const Color coral = Color(0xFFFF6F61);      // Coral
  static const Color darkGray = Color(0xFF333333);   // Dark Gray

  // Colors for Dark Theme
  static const Color darkPrimaryBlue = Color(0xFF1C3A63);  // Darker Blue for Dark Mode
  static const Color darkPrimaryTeal = Color(0xFF00695C);  // Darker Teal
  static const Color darkGrayBackground = Color(0xFF121212);  // Dark background color
  static const Color darkSurface = Color(0xFF1E1E1E);  // Surface color for dark mode
  static const Color darkText = Color(0xFFE0E0E0);     // Light text for dark mode
  static const Color darkCoral = Color(0xFFFF5C5C);    // Slightly darker Coral for dark mode

  // Shared Colors
  static const Color errorRed = Color(0xFFD32F2F);   // Error messages
}
